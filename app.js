'use strict';
/**
 * @module app.js
 *
 * The main app initializer. All of the app initialization logic is controlled
 * here, meaning the order in which different components are initialized. A
 * promise should be returned when everything is initialized so that that we can
 * know when **everything** is initialized.
 *
 * @todo TODO Write the initialization code
 */
